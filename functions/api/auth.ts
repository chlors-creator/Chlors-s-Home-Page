interface Env { ADMIN_USERNAME: string; ADMIN_PASSWORD: string }
const cookieName = 'console_session';
const json = (body: unknown, status = 200, headers: HeadersInit = {}) => new Response(JSON.stringify(body), { status, headers: { 'content-type': 'application/json; charset=utf-8', ...headers } });
const encode = (value: string) => btoa(value).replaceAll('+', '-').replaceAll('/', '_').replaceAll('=', '');
async function signature(value: string, secret: string) { const key = await crypto.subtle.importKey('raw', new TextEncoder().encode(secret), { name: 'HMAC', hash: 'SHA-256' }, false, ['sign']); return encode(String.fromCharCode(...new Uint8Array(await crypto.subtle.sign('HMAC', key, new TextEncoder().encode(value))))); }
async function sessionValue(username: string, secret: string) { const payload = `${username}:${Date.now()}`; return `${encode(payload)}.${await signature(payload, secret)}`; }
export async function isAuthenticated(request: Request, env: Env) { const raw = request.headers.get('Cookie')?.match(new RegExp(`${cookieName}=([^;]+)`))?.[1]; if (!raw) return false; const [payload, sig] = raw.split('.'); if (!payload || !sig) return false; let decoded = ''; try { decoded = atob(payload.replaceAll('-', '+').replaceAll('_', '/') + '=='); } catch { return false; } const [username, stamp] = decoded.split(':'); if (username !== env.ADMIN_USERNAME || Date.now() - Number(stamp) > 86400000) return false; return (await signature(decoded, env.ADMIN_PASSWORD)) === sig; }
export const onRequest = async ({ request, env }: { request: Request; env: Env }) => { const url = new URL(request.url); if (request.method === 'GET') return json({ authenticated: await isAuthenticated(request, env) }); if (request.method !== 'POST') return json({ error: '方法不允许。' }, 405); const input = await request.json().catch(() => ({})) as Record<string, string>; if (input.action === 'logout') return json({ ok: true }, 200, { 'Set-Cookie': `${cookieName}=; Max-Age=0; Path=/; HttpOnly; Secure; SameSite=Lax` }); if (input.username !== env.ADMIN_USERNAME || input.password !== env.ADMIN_PASSWORD) return json({ error: '账号或密码错误。' }, 401); return json({ ok: true }, 200, { 'Set-Cookie': `${cookieName}=${await sessionValue(env.ADMIN_USERNAME, env.ADMIN_PASSWORD)}; Max-Age=86400; Path=/; HttpOnly; Secure; SameSite=Lax` }); };
